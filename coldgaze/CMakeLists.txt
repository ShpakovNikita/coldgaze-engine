cmake_minimum_required(VERSION 3.0.2)

set(PROJECT_NAME coldgaze.framework)
set(PROJECT_VERSION 0.0.1)
set(PACKAGE_NAME ${PROJECT_NAME}-${PROJECT_VERSION}-${PLATFORM})

message(STATUS ${PACKAGE_NAME})

project(${PACKAGE_NAME})

